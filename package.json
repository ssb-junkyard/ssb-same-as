{
  "name": "ssb-same-as",
  "description": "A scuttlebot plugin that provides a stream of which feeds are (and are not) the same as other feeds.",
  "version": "0.0.0",
  "homepage": "https://github.com/ssbc/ssb-same-as",
  "repository": {
    "type": "git",
    "url": "git://github.com/ssbc/ssb-same-as.git"
  },
  "dependencies": {
    "flumeview-reduce": "^1.3.0",
    "graphreduce": "^3.0.3",
    "pull-flatmap": "0.0.1",
    "pull-stream": "^3.6.0",
    "ssb-friends": "^2.3.5",
    "ssb-ref": "^2.7.1"
  },
  "devDependencies": {
    "scuttlebot": "^10.3.0",
    "tape": "^4.6.3"
  },
  "scripts": {
    "test": "set -e; for t in test/*.js; do node $t; done"
  },
  "author": "Matt McKegg",
  "license": "MIT"
}
